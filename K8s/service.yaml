apiVersion: "v1"
kind: "Service"
metadata:
  name: "travel-app"
  namespace: "travel"
  labels:
    app: "frontend"
spec:
  ports:
    - protocol: "TCP"
      port: 5050
      targetPort: 5000
  selector:
    app: "frontend"
  type: "ClusterIP"
