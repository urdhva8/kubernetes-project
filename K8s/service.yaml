apiVersion: "v1"
kind: "Service"
metadata:
  name: "travel-app"
  namespace: "travel"
  labels:
    app: "frontend"
spec:
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5050
  selector:
    app: "frontend"
  type: "ClusterIP"
